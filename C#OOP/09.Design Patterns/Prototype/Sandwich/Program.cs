using System;

namespace Sandwich
{
    public class Program
    {
        static void Main(string[] args)
        {
            /*Sandwich fstSandwich = new Sandwich("Wheat", "Bacon", "Lettuse", "Tomato", 15);
            
            Sandwich sallowCopy = fstSandwich.ShallowCopy();
            Sandwich deepCopy = fstSandwich.DeepCopy();

            sallowCopy.Weight.Gr = 20; 
            deepCopy.Weight.Gr = 35; 

            Console.WriteLine($"{nameof(fstSandwich)} {fstSandwich.Weight.Gr}");
            Console.WriteLine($"{nameof(sallowCopy)} {sallowCopy.Weight.Gr}");
            Console.WriteLine($"{nameof(deepCopy)} {deepCopy.Weight.Gr}");*/
        }
    }
}
